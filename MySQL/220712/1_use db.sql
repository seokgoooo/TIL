-- Ctrl + Shift + Enter ( 전부 다 실행 )
-- Ctrl + Enter ( 커서의 한 문장 실행 )
-- 주석 MySQL 은 문자열 합연산이 안 된다.

SHOW DATABASES;

USE world;

SHOW TABLES;

SELECT 3 % 2;

SELECT 'Hello world';

SELECT 10 = 10;		--  관계연산자 : ( = ) 하나만 사용해도 같냐? 라는 말 True 면 1 False 면 0 반환

SELECT 10 <> 11;	-- != 도 되지만 보통 <> 을 쓴다