```
1. 회원가입 페이지에서 나이 입력하는 부분을
JFormattedTextField로 구현해봤는데
내 생각은 1 ~ 100 까지 숫자 범위를 설정해주면
그 사이의 숫자만 입력되게 제한시켜줄거라고 생각했지만
이걸 NumberFormatException 예외로 잡아서 처리하려고 해서 그런지
이 친구는 Integer 타입인지 아닌지가 중요하고
Integer면 범위의 숫자를 초과하더라도 그대로 입력을 시켜주는 역할을 하더라
If를 원하는만큼 써주면 쉽게 해결 될수있겠지만 여태 뭐만하면 무지성 If를 쓰는거 같아서
이번에는 최대한 If를 안 써보고 싶어서 찾아본게
JFormattedTextField 클래스에 있는 setFocusLostBehavior 메소드다.
상수로 COMMIT REVERT PERSIST COMMIT_OR_REVERT 4가지가 있다.
TextField에 있던 마우스 Focus가 다른 곳으로 가게 되었을 때
TextField에 입력되어 있는 값이 ( 범위에 맞는지 / 원하는 타입인지 ) 확인하고
False면 이전의 True인 값으로 되돌리기, True False 상관 없이 값을 그대로 유지하기 등등
여러가지 처리 방법이 있는데
나는 Auto로 설정되어 있는 False 값이 입력되고 Focus가 벗어났으면 이전의 True값으로 되돌리는 방법으로
나이 Field를 제한시키는 방법을 써봤다.

2. 이슈까진 아닌데 궁금한 부분
Ranking 화면에서 구현된 Tab에 들어가는 panel은 4개다.
각각의 panel은 반복되는 부분이 많다고 생각해서
1개의 panel을 만드는 메소드를 만들어서 4번 사용하고 싶었는데
메소드 만드는걸 하루종일 해봐도 안 되더라
아마 내 생각엔 참조하는 부분에서 문제가 있을거라 생각한다.
시간이 부족해서 메소드에서 클래스로 바꿔서 1개의 panel을 만드는 클래스를 만들어
4개의 panel 객체를 만들어 Tab에 넣어주는 방식으로 구현했다.
어떻게해야 메소드를 이용해서 만들 수 있을까 싶은 생각과
아니면 원래 메소드로 안 만들어지는 부분인가 싶기도 하다
